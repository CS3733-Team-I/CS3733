package FoodRequestAPI.utility.request;

// NOTE don't change the order of these it'll mess up the FoodRequestAPI.database
public enum RequestType {
    GENERAL,
    INTERPRETER,
    FOOD,
    SECURITY,
    JANITOR,
    NONE
}
